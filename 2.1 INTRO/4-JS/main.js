'use strict';

const mainTitle = document.querySelector('.title');

const selection = document.querySelector('.item').innerHTML;
const selection2 = document.querySelector('.item2');

mainTitle.innerHTML = mainTitle.innerHTML + selection;
